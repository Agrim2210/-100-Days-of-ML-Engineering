# Conceptual Foundations — Learning Curves

Learning curves plot model performance against training dataset size.

Two curves are analyzed:

Training Score:
Performance on training data.

Validation Score:
Performance on unseen validation data.

The gap between these curves indicates model behavior:

• Small gap → good generalization  
• Large gap → overfitting  
• Both low → underfitting