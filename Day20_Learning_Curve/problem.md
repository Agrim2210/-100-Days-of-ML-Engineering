# Day 20 — Learning Curves

## Problem

Model performance metrics alone do not explain whether a model is underfitting or overfitting.

Without proper diagnosis, improving model performance becomes difficult.

## Objective

This experiment uses learning curves to analyze model performance as training data size increases.

The goal is to identify underfitting and overfitting by comparing training and validation scores.