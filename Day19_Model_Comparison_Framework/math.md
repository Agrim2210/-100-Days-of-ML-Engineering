# Day 19 — Model Comparison Framework

## Problem

Choosing a machine learning model based on a single train-test split can lead to unreliable conclusions.

Different models may perform differently depending on the data split.

## Objective

This experiment evaluates multiple models using
cross-validation.

The goal is to compare models systematically and
select the best-performing model based on
average performance across multiple folds.