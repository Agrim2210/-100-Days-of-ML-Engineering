# Conceptual Foundations — Categorical Encoding

Categorical encoding converts non-numeric data into numerical form.

Label Encoding assigns integer values to categories.

One Hot Encoding creates binary vectors for each category, ensuring no ordinal relationship is introduced.

Improper encoding can introduce bias in models that assume numerical relationships between feature values.

Choosing the correct encoding method is essential for accurate model performance.